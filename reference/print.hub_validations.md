# Print results of `validate_...()` function as a bullet list

Print results of `validate_...()` function as a bullet list

## Usage

``` r
# S3 method for class 'hub_validations'
print(x, ...)
```

## Arguments

- x:

  An object of class `hub_validations` or any of it's subclasses.

- ...:

  Unused argument present for class consistency
