# Concatenate `target_validations` S3 class objects

Concatenate `target_validations` S3 class objects

## Usage

``` r
# S3 method for class 'target_validations'
combine(...)
```

## Arguments

- ...:

  `target_validations` S3 class objects to be concatenated.

## Value

a `target_validations` S3 class object.
