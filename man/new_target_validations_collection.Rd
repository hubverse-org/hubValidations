% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/new_target_validations.R
\name{new_target_validations_collection}
\alias{new_target_validations_collection}
\alias{as_target_validations_collection}
\title{Create new or convert list to \code{target_validations_collection} S3 class object}
\usage{
new_target_validations_collection(...)

as_target_validations_collection(x)
}
\arguments{
\item{...}{\code{target_validations} objects to be included.}

\item{x}{a list where each element is a \code{target_validations} object.}
}
\value{
an S3 object of class \verb{<target_validations_collection>}. Elements are
named by their \code{where} attribute
(e.g., \code{collection[["path/to/file.csv"]]}).
}
\description{
A \code{target_validations_collection} is a container for target validation results
from multiple validation subjects. It is a named list where each element is a
\code{target_validations} object. Names are automatically extracted from the
\code{where} attribute of each \code{target_validations} object. If multiple
\code{target_validations} objects have the same \code{where} value, they are merged using
\code{\link[=combine]{combine()}}. Empty \code{target_validations} objects are ignored.
}
\section{Functions}{
\itemize{
\item \code{new_target_validations_collection()}: Create new
\verb{<target_validations_collection>} S3 class object

\item \code{as_target_validations_collection()}: Convert list to
\verb{<target_validations_collection>} S3 class object

}}
\examples{
new_target_validations_collection()

# Create validations for two different files
file_path_1 <- "time-series.csv"
validations_1 <- new_target_validations(
  target_file_name = check_target_file_name(file_path_1),
  target_file_ext_valid = check_target_file_ext_valid(file_path_1)
)

file_path_2 <- "other-data.csv"
validations_2 <- new_target_validations(
  target_file_name = check_target_file_name(file_path_2),
  target_file_ext_valid = check_target_file_ext_valid(file_path_2)
)

# Combine into a collection
collection <- new_target_validations_collection(validations_1, validations_2)

# Print the collection
collection

# Get file paths (element names)
names(collection)

# Access validations for a specific file
collection[[file_path_1]]

# Access a specific check within a file's validations
collection[["time-series.csv"]]$target_file_name
}
